# pow()
## NAME
**pow** - find an exponent of a float

## SYNOPSIS
~~~cxx
float pow( float x, float y );
~~~

## DESCRIPTION
pow() returns x to the y power.  If x is 0.0, y must be
positive.  If x is negative, y must be an integer.

## SEE ALSO
[exp()](exp.md), [log()](log.md), [sqrt()](sqrt.md)
