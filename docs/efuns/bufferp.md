# bufferp()
## NAME
**bufferp** - determine whether or not a given variable is a buffer

## SYNOPSIS
~~~cxx
int bufferp (mixed arg);
~~~

## DESCRIPTION
Return `1` if **arg** is a buffer value and zero (`0`) otherwise.

## SEE ALSO
[mapp()](mapp.md), [stringp()](stringp.md), [pointerp()](pointerp.md), [objectp()](objectp.md), [intp()](intp.md), [bufferp()](bufferp.md), [floatp()](floatp.md), [functionp()](functionp.md), [nullp()](nullp.md), [undefinedp()](undefinedp.md), [errorp()](errorp.md)
