name: "CodeQL Deep"

on:
  push:
    branches: [ "main" ]
    paths:
      - '**.c'
      - '**.cpp'
      - '**.cmake'
      - '**.h'
      - '**.hpp'
      - '**.y'
      - 'CMakeLists.txt'
  schedule:
    - cron: '35 7 * * 5'

jobs:
  analyze:
    name: Analyze (c-cpp on ubuntu-latest)
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read

    steps:
    - name: Checkout repository
      uses: actions/checkout@v6

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v4
      with:
        languages: c-cpp
        build-mode: manual
        config: |
          paths-ignore:
            - out/

    # Cache CMake FetchContent dependencies (source code and build files)
    # 
    - name: Cache build dependencies
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      uses: actions/cache@v5
      with:
        path: |
          out/build/linux/_deps
        key: ${{ runner.os }}-deps-${{ hashFiles('cmake/*.cmake', 'CMakeLists.txt') }}
        restore-keys: |
          ${{ runner.os }}-deps-

    # Cache boost apt package archives (binary libraries) to speed up installation on subsequent runs
    # Boost components are listed in CMakeLists.txt, so we can use that to determine the cache key
    - name: Cache Boost apt package archives
      uses: actions/cache@v5
      with:
        path: |
          /var/cache/apt/archives
          /var/lib/apt/lists
        key: ${{ runner.os }}-apt-boost-${{ hashFiles('CMakeLists.txt') }}
        restore-keys: |
          ${{ runner.os }}-apt-boost-

    - name: Install Boost on Ubuntu
      shell: bash
      run: |
        set -euo pipefail
        sudo apt-get update
        if apt-cache show libboost-json-dev >/dev/null 2>&1; then
          sudo apt-get install -y --no-install-recommends libboost-json-dev
        elif apt-cache show libboost-json1.83-dev >/dev/null 2>&1; then
          sudo apt-get install -y --no-install-recommends libboost-json1.83-dev
        else
          sudo apt-get install -y --no-install-recommends libboost-all-dev
        fi

    # In github-hosted runner 'ubuntu-latest', we expect these packages are already installed:
    # - bison
    # - cmake
    # - curl (libcurl)
    # - g++ / gcc
    # - jq (libjq1)
    # - openssl (libssl, libcrypto)
    - name: Run manual build steps
      shell: bash
      run: |
        cmake --preset linux \
          -DFETCH_GOOGLETEST_FROM_SOURCE=v1.17.0 \
          -DFETCH_CARES_FROM_SOURCE=v1.34.6
        cmake --preset linux -LA
        cmake --build --preset ci-linux --clean-first

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v4
      with:
        category: "/language:c-cpp"